// I really have no clue how to use jest, I do have it install but it is incredibly confusing.
//////*** If this could be explained. please help, thank you! ***/////

const onSubmit = require('../app')

describe('testing function', () => {
    test('tests submit', () => {
        expect(onSubmit).toBeDefined()
    })
})
